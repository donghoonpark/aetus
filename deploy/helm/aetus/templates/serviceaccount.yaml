{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "aetus.serviceAccountName" . }}
  labels:
    {{- include "aetus.labels" . | nindent 4 }}
{{- end }}
